# Система подписок и администрирования

## Обзор

Реализована полная система платных подписок с интеграцией ЮКасса и панелью суперадминистратора для управления платформой.

## Тарифные планы

### Free (Бесплатный)
- **Цена**: 0 ₽
- **Лимит товаров**: 10 товаров на бота
- **Возможности**:
  - До 10 товаров
  - Базовый функционал
  - Email поддержка

### Premium (Премиум)
- **Цена**: 1990 ₽/месяц
- **Лимит товаров**: Безлимит
- **Возможности**:
  - Безлимитное количество товаров
  - Приоритетная поддержка
  - Расширенная аналитика
  - API доступ

## Автоматическое назначение подписок

- При регистрации каждый новый пользователь автоматически получает Free подписку на 1 год
- Первый зарегистрированный пользователь (skandal1inc@gmail.com) автоматически стал superadmin
- Все существующие пользователи были мигрированы на Free план с подпиской на 1 год

## Проверка лимитов

Система автоматически проверяет лимиты при:
- Создании нового товара через интерфейс
- Массовой загрузке товаров из Excel

Если лимит достигнут, пользователь получит сообщение:
> "Достигнут лимит товаров для вашего тарифа. Обновите подписку для добавления большего количества товаров."

## Роли пользователей

### User (Обычный пользователь)
- Доступ ко всем функциям создания и управления ботами
- Управление товарами, категориями, заказами
- Просмотр статистики по своим ботам
- Управление своей подпиской

### Superadmin (Суперадминистратор)
Дополнительно к функциям обычного пользователя:
- Управление всеми пользователями платформы
- Просмотр статистики платформы
- Блокировка/разблокировка пользователей
- Изменение ролей пользователей
- Ручное назначение подписок

## Панель суперадминистратора

### Управление пользователями (`/admin/users`)
Функции:
- Поиск пользователей по email
- Фильтрация по статусу (активные/заблокированные)
- Просмотр детальной информации о пользователе
- Блокировка/разблокировка пользователей
- Изменение роли (user ↔ superadmin)
- Ручное назначение подписки (план + длительность)
- Просмотр количества ботов и платежей пользователя

### Статистика платформы (`/admin/stats`)
Показатели:
- **Общая статистика**:
  - Всего пользователей
  - Всего ботов
  - Активные подписки
  - Общая выручка (₽)
- **Новые пользователи** за выбранный период (7/30/90 дней)
- **Распределение подписок** по планам
- **График роста пользователей** (последние 30 дней)

## Интеграция с ЮКасса

### Настройка

1. Зарегистрируйтесь на https://yookassa.ru/
2. Получите `SHOP_ID` и `SECRET_KEY` в личном кабинете
3. Добавьте в `backend/.env`:
```env
YOOKASSA_SHOP_ID="your_shop_id"
YOOKASSA_SECRET_KEY="your_secret_key"
YOOKASSA_WEBHOOK_URL="https://yourdomain.com/api/webhooks/yookassa"
```

4. Настройте webhook в личном кабинете ЮКассы:
   - URL: `https://yourdomain.com/api/webhooks/yookassa`
   - Событие: `payment.succeeded`

### Flow оплаты

1. Пользователь выбирает тариф на странице `/subscription`
2. Нажимает "Перейти на этот план"
3. Система создает платеж в ЮКассе
4. Пользователь перенаправляется на страницу оплаты ЮКассы
5. После оплаты ЮКасса отправляет webhook
6. Backend активирует подписку
7. Пользователь возвращается на сайт с активной подпиской

## Автоматическая проверка истекших подписок

Система автоматически запускает cron job каждые 24 часа:
- Находит все подписки с `endDate < now()` и `status = 'active'`
- Переводит их в статус `expired`
- Пользователи с истекшей подпиской автоматически переходят на Free план

В логах можно увидеть:
```
🔄 Subscription cron job started (runs every 24 hours)
ℹ️  No expired subscriptions found
```

## Страницы и компоненты

### Frontend

#### `/subscription` - Управление подпиской
- Отображение текущего плана
- Progress bar использования лимита товаров
- Карточки всех доступных тарифов
- Кнопка перехода на другой план
- История платежей

#### `/admin/users` - Управление пользователями (только superadmin)
- Таблица всех пользователей
- Поиск и фильтрация
- Модальные окна для действий
- Отображение подписок и статистики по пользователям

#### `/admin/stats` - Статистика платформы (только superadmin)
- Карточки с ключевыми метриками
- График роста пользователей
- Распределение подписок

#### Navigation - Боковая навигация
- Автоматически подстраивается под роль пользователя
- Для superadmin добавляется раздел "Администрирование"
- Адаптивная подсветка активного раздела

### Backend API

#### Подписки (`/api/subscriptions`)
- `GET /plans` - Список доступных тарифов
- `GET /current` - Текущая подписка и использование лимитов
- `POST /checkout` - Создание платежа (перенаправление на ЮКассу)
- `GET /payments` - История платежей пользователя

#### Webhooks (`/api/webhooks`)
- `POST /yookassa` - Обработка уведомлений от ЮКассы

#### Администрирование (`/api/admin`)
Все маршруты защищены middleware `requireSuperAdmin`

**Пользователи:**
- `GET /users` - Список пользователей (с поиском и фильтрацией)
- `GET /users/:id` - Детальная информация о пользователе
- `PUT /users/:id/block` - Блокировка/разблокировка
- `PUT /users/:id/role` - Изменение роли

**Подписки:**
- `POST /subscriptions/assign` - Ручное назначение подписки
- `PUT /subscriptions/:id/cancel` - Отмена подписки

**Статистика:**
- `GET /stats` - Статистика платформы (с фильтром по периоду)

## База данных

### Новые модели

#### SubscriptionPlan
```prisma
model SubscriptionPlan {
  id          String   @id @default(uuid())
  name        String   @unique         // free, premium
  displayName String                   // Free, Premium
  price       Decimal  @db.Decimal(10, 2)
  maxProducts Int                      // -1 = unlimited
  features    Json     @default("[]")  // ["Feature 1", "Feature 2"]
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
}
```

#### Subscription
```prisma
model Subscription {
  id          String    @id @default(uuid())
  userId      String
  planId      String
  status      String    @default("active")  // active, expired, cancelled, pending
  startDate   DateTime  @default(now())
  endDate     DateTime
  autoRenew   Boolean   @default(false)
  cancelledAt DateTime?
}
```

#### Payment
```prisma
model Payment {
  id                String    @id @default(uuid())
  userId            String
  subscriptionId    String?
  amount            Decimal   @db.Decimal(10, 2)
  status            String    @default("pending")  // pending, succeeded, cancelled, failed
  yookassaPaymentId String?   @unique
  yookassaData      Json?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
}
```

### Обновления модели User
```prisma
model User {
  role       String  @default("user")      // user, superadmin
  isBlocked  Boolean @default(false)
  
  subscriptions Subscription[]
  payments      Payment[]
}
```

## Middleware

### `authenticate` (обновлен)
- Проверяет JWT токен
- Загружает роль и email пользователя
- Проверяет блокировку (`isBlocked`)
- Добавляет в request: `userId`, `userRole`, `userEmail`

### `requireSuperAdmin` (новый)
- Проверяет, что роль пользователя = `superadmin`
- Возвращает 403 для обычных пользователей

### `checkProductLimit` (новый)
- Получает активную подписку пользователя
- Проверяет лимит товаров (`maxProducts`)
- Если `-1` - пропускает (безлимит)
- Если достигнут лимит - возвращает 403 с сообщением об ошибке

## Тестирование

### Проверка регистрации
1. Зарегистрируйте нового пользователя
2. Проверьте, что ему автоматически назначена Free подписка
3. Попробуйте создать более 10 товаров - должна быть ошибка

### Проверка Upgrade
1. Войдите как обычный пользователь
2. Перейдите в `/subscription`
3. Нажмите "Перейти на Premium план"
4. (Для тестирования можно использовать тестовую карту ЮКассы)

### Проверка Superadmin
1. Войдите как superadmin (skandal1inc@gmail.com)
2. В навигации должен появиться раздел "Администрирование"
3. Перейдите в `/admin/users`
4. Попробуйте заблокировать пользователя
5. Попробуйте назначить Premium подписку вручную
6. Перейдите в `/admin/stats` для просмотра статистики

### Проверка блокировки
1. Заблокируйте пользователя через admin panel
2. Попробуйте войти под этим пользователем
3. Должна быть ошибка: "Account is blocked"

## Безопасность

- ✅ Rate limiting на webhook endpoints
- ✅ Проверка роли для admin endpoints
- ✅ Проверка владения ботов перед проверкой лимитов
- ✅ JWT токен с expiration (7 дней)
- ✅ Валидация данных через Zod (в некоторых маршрутах)
- ✅ CORS настроен только для разрешенных доменов

## Следующие шаги (опционально)

1. **Email уведомления**:
   - При успешной оплате
   - За 3 дня до истечения подписки
   - При истечении подписки

2. **Автопродление подписок**:
   - Возможность включить autoRenew
   - Автоматическое списание средств

3. **Промокоды**:
   - Скидки на подписки
   - Бесплатные пробные периоды

4. **Годовая подписка**:
   - Premium на 12 месяцев со скидкой

5. **Дополнительные метрики**:
   - Лимиты на заказы в месяц
   - Лимиты на количество клиентов
   - Дополнительные боты

## Известные ограничения

- YooKassa credentials в `.env` пока пустые - нужно заполнить после регистрации
- Email уведомления пока не реализованы
- Автопродление подписок отключено (`autoRenew: false`)
- Возврат средств нужно делать вручную через ЮКассу

## Техническая поддержка

При возникновении проблем проверьте:
1. Логи backend сервера
2. Webhook уведомления в личном кабинете ЮКассы
3. Статус подписки в базе данных
4. Корректность .env переменных

---

**Дата создания**: 28 декабря 2025
**Версия**: 1.0.0
